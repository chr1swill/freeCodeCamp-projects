// *** look through array of objects return object with matching key and value pair ***
// loop through array of objects
// check if key and value match
// if not matching continue
// if match return object
//else return no matching object 