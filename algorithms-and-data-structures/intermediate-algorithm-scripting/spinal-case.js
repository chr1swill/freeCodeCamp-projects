// split string by capital letters or "-" or "_"
// set that that the array to lowercase using map
// loop through the array
// if it is the last element of the array do nothing
// else current index + "-"
// join the array back together